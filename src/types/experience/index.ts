export type ExperienceTypes = {
    title: string;
    company: string;
    date: string;
    descriptionList: string[];
    skills: string[];
}

export type ExperienceResponse = Array<ExperienceTypes>;